# Ferramentas

Detran PE: Guias de IPVA expõe 1 ferramenta (todas somente leitura).

### 1. `detran_pe_guias_ipva_consultar`
**Input**: `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional), `placa`, `cpf_proprietario` (opcional), `cnpj_proprietario` (opcional)

Detran PE: Guias de IPVA, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Detran PE: Guias de IPVA
```
